<?xml version="1.0" encoding="utf-8" standalone="no"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.apkscanner.nativecases"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="36" />

    <application
        android:allowBackup="false"
        android:debuggable="false"
        android:label="APKScanner Native Cases"
        android:theme="@android:style/Theme.Material.Light">

        <activity
            android:name=".NativeGateActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="io.apkscanner.nativecases.ADMIN_ACTION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DynamicLinkActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="nativecases" android:host="elevate" />
            </intent-filter>
        </activity>

        <service
            android:name=".NativeSecretService"
            android:exported="true">
            <intent-filter>
                <action android:name="io.apkscanner.nativecases.READ_NATIVE_SECRET" />
            </intent-filter>
        </service>
    </application>
</manifest>
